include(ExternalProject)

# Add HDT
ExternalProject_Add(hdt
# DOWNLOAD_DIR "${EXT_PROJECTS_DIR}/hdt"
GIT_REPOSITORY "https://github.com/rdfhdt/hdt-cpp.git"
GIT_TAG "master"
SOURCE_DIR "${EXT_PROJECTS_DIR}/hdt/hdt-lib"
BUILD_COMMAND make
)
#set(HDT_INCLUDE_DIRS "${CMAKE_SOURCE_DIR}/ext/hdt/hdt-lib/include")
#include_directories(${HDT_INCLUDE_DIRS})
